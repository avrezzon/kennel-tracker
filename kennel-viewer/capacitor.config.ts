import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kennel-viewer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
